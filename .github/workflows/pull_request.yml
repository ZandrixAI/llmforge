name: Build and Test

on:
  push:
    branches: ["main"]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/head/main' }}

jobs:
  check_lint:
    if: github.repository == 'llmforge/llmforge'
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.10"
      - uses: pre-commit/action@v3.0.1

  build_and_test:
    if: github.repository == 'llmforge/llmforge'
    runs-on: ubuntu-22.04
    needs: check_lint
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.10"
      - name: Install test dependencies
        run: |
          pip install unittest-xml-reporting
          pip install -e ".[test]"
      - name: Run tests
        run: |
          curl -o test_data.zip -L https://github.com/llmforge/llmforge/releases/download/test_data/test_data.zip
          unzip test_data.zip
          HF_HOME="." python -m xmlrunner discover -v tests -o test-results/
